[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
markers = [
    "integration: requires the compiled compileone backend (make all)",
]

[tool.ruff]
line-length = 110
target-version = "py313"
